/**
 * When Carz AI reaches the App Store.
 *
 * Kept out of any component so the homepage's countdown and the copy around it
 * read the same date, and swapping the countdown component doesn't take the
 * date with it.
 */
export const RELEASE_DATE = "2026-10-05T00:00:00";

/** The same day, written out. Beside the date so the two cannot disagree. */
export const RELEASE_LABEL = "October 5, 2026";
